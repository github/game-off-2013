define(function (require) {
    'use strict';

    var splat;

    beforeEach(function() {
        splat = require('splat');


    });

    describe('generate', function() {
//        it ('should generate a polygon with the correct number of points', function() {
//            var points = 10;
//
//            var result = splat.generate(0, 0, 100, points);
//
//            expect(result.type).toBe('Polygon');
//            expect(result.coordinates.length).toBe(points + 1);
//            expect(result.coordinates[0]).toBe(result.coordinates[points]);
//        });
    })
});